name: The Real RDP (Corrected)

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    steps:
      - name: 1. Setup RDP Server
        # This is the corrected action name
        uses: P3TERX/rdp-github-action@v1.1
        with:
          ngrok_token: ${{ secrets.NGROK_AUTH_TOKEN }}
          username: user
          password: StrongPassword123!

      - name: 2. Keep Alive
        run: Start-Sleep -Seconds 36000
